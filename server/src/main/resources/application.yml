server:
  port: 8080

spring:
  application:
    name: smart-store-server
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

management:
  endpoints:
    web:
      exposure:
        include: health

smart-store:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}
  jwt:
    secret: ${JWT_SECRET}
  wechat:
    app-id: ${WECHAT_APP_ID}
    app-secret: ${WECHAT_APP_SECRET}
    local-mock-enabled: ${WECHAT_LOCAL_MOCK_ENABLED:false}
    local-mock-openid: ${WECHAT_LOCAL_MOCK_OPENID:local-dev-customer}
  store-contact:
    phone: ${STORE_PHONE:18653045492}
    customer-service-enabled: ${WECHAT_CUSTOMER_SERVICE_ENABLED:true}
  upload-dir: ${UPLOAD_DIR}
  rate-limit:
    staff-login:
      limit: 10
      window-seconds: 600
    customer-login:
      limit: 30
      window-seconds: 600
    order-create:
      limit: 10
      window-seconds: 60
